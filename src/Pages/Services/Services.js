import React from 'react';
import useTitle from '../../Hooks/UseTitle';

const Services = () => {
    useTitle('Services')
    return (
        <div>
            This is services
        </div>
    );
};

export default Services;